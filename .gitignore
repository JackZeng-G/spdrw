# ---------- 构建缓存 ----------
# 容器内必须自定这两个目录(默认位置不可写), 见 docs/实现文档.md §7
.gocache/
.gopath/

# ---------- 构建产物 ----------
# 注意: build/ 下**不全是**产物 ——
#   build/icon/    图标源(由 go run ./tools/makeicon 生成, 要入库)
#   build/winres/  Windows 资源(图标 + 版本信息)配置, 要入库
# 只有编译出来的可执行文件/库/临时文件才忽略。
build/*.exe
build/*.dll
build/*.tmp
build/*.pdb
build/*.log

# ---------- 临时文件与生成物 ----------
# 曾被当成测试 HOME 的仓库内临时目录(Go telemetry / ~/.spdrw/backups 会落到里面);
# 现在测试自己把 HOME 指到系统临时目录, 这里再兜一层, 免得混进提交。
.tmphome/
_mock.html
*.tmp
*.orig
*.rej
# 冒烟页由 make-real-page.mjs 从 dist/index.html 现生成(见 frontend/test/README.md)
frontend/test/_real-page.html
