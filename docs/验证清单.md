# 真机验证清单

在真实硬件上部署前逐项确认。所有"读取"类操作无风险;"写入/保护"类操作请先读懂每一步。

> 已完成的实测与原始证据(日志行)记在 `docs/真机实测记录.md`;本节勾选进度同步在那里。

## 0. 环境

- [ ] Windows 10/11 x64, 已安装 [PawnIO](https://pawnio.eu/) 最新版
- [ ] 以管理员身份运行 `SPDReaderWriter.exe`
- [ ] 主界面右上角显示 "就绪 · N 个控制器", 无黄色警告条
- [ ] (可选) BIOS 中 "SPD Write Disable" 关闭(写入前必须)

## 1. 控制器枚举(只读)

- [ ] Intel 平台: 出现 "Intel PCH SMBus (I801)" 且显示 PCI 地址
- [ ] AMD 平台: 出现 KernCZ PIIX4 (可能 2 个端口条目)
- [ ] 服务器 Skylake-SP: 出现 "Skylake IMC SMBus" ×2
- [ ] 与 CPU-Z / HWiNFO 的 SMBus 控制器 PCI ID 一致

## 2. 扫描与读取(只读)

- [x] 每根内存条出现在 0x50-0x57, 数量与插槽数一致 —— 实测 `发现 2 个 SPD 设备`
- [x] 类型识别正确(DDR4 显示 DDR4, DDR5 显示 DDR5) —— 实测 `0x50 (DDR5, 1024 字节)`
- [x] "读取" 得到 512B(DDR4)/1024B(DDR5) —— 实测 1024 字节读取完成
- [x] 日志写明读取方式与事务数: 形如
      `读取 1024 字节: 块读(32 字节/事务); 事务 33 次, 耗时 1.0s(等待 0 ms)`,
      或字读档 `… 字读(2 字节/事务); 事务 520 次, 耗时 16s …`, 或
      `… 逐字节(1 字节/事务); 事务 1024 次, 耗时 32s …`(最后一种还会带"回退原因: …")。
      **把这一行回帖**即可对比提速效果(读路径按 块读 32B → 字读 2B → 逐字节 1B 自动降级)
- [ ] (可选)取消勾选"读加速"再读一次, 对比耗时; 若加速档数据与逐字节不一致, 请把两条日志都回帖
- [ ] 信息面板: 厂商/部件号/序列号/生产日期与 CPU-Z、Thaiphoon 一致
- [ ] 容量与实际一致(例: 2×16GB 双列显示 16 GiB)
- [ ] DDR4: CRC 校验通过(若失败, dump 可能分页错位 — 重读一次)
- [ ] DDR4 时序: tCL/tAA/tRCD/tRP/tRAS 与 BIOS/CPU-Z 一致
- [ ] XMP 条目: 频率-时序-电压与包装标签一致
- [ ] DDR5: CRC(基础段)通过; XMP/EXPO 存在性正确
- [ ] (对照) DDR2/DDR3 老平台: 容量与部件号正确

## 3. 写保护状态(只读; DDR4 及更早无状态寄存器, 查询会对块首做"写测试"= 写入取反值再还原)

- [x] "查询保护状态"返回块列表并显示字节范围: DDR5 = 16 块 × 64B, DDR4 = 4 块 × 128B, DDR3 = 1 块 × 128B
      —— 实测正常(修复前该按钮因绑定返回值超限返回 null)
- [ ] 出厂未保护的条: 全部 🔓; 已 RSWP 的块显示 🔒(可先用别条测试)
- [ ] DDR5: MR12/MR13 原始值与 Thaiphoon/参考工具一致; `MR52[6]=1` 表示"最近有写受保护块被忽略"
- [x] DDR5: **不再出现**"PSWP 永久保护已生效"—— 该世代无 0110b(PWPB)设备类型, 面板应显示"PSWP 不适用"
      —— 实测正常(修复前误报)
- [ ] DDR2/DDR3: PSWP 未设置/已永久保护 与实机一致(永久保护后器件不再应答 0x30|SA)
- [ ] DDR4 等: 若某块写测试失败, 该块显示 ❔ 未知(而不是谎报受保护), 日志给出"还原失败 + 偏移 + 原值"警告
- [ ] 查询后重新 dump, 内容与查询前逐字节一致(写测试确实还原了)

## 3.5 SPD 编辑器(纯内存预览, 未"应用到设备"前不碰总线)

- [x] 编辑器页显示来源(dump 文件 / 从设备载入)与"变更 N 字节"面板 —— 实测正常
- [ ] 分组页签按需渲染(常用信息 / JEDEC 时序 / XMP 2.0 / XMP 3.0 / EXPO), 一次只画一组
- [x] 十六进制格内就地编辑: 输入 `5A` 即 0x5A(`0x5A` 亦可), 输入 `0b` 即 0x0B —— **按十六进制解析**
      (旧版把它当十进制, `5A` 会变成 5、`0b` 变成 0) —— 实测 `原始编辑: 0x203 = 0x5A`
- [x] 改字节后日志按"是否影响校验"给出提示(影响 → 提示重算 CRC; 不影响 → 明确写"无需重算")
- [x] 左上角标题旁**实时校验状态**: 读一份正常 dump 显示"CRC 通过"(悬停可看校验区划分与不参与校验的区域)
- [ ] 改一个**校验范围内**的字节(如 JEDEC 时序)→ 状态变"CRC 需重算", 该格标红
- [ ] 改**序列号/生产日期**(0x143-0x148)→ 状态仍是"CRC 通过 · N 处改动不在校验范围", 该格标蓝
- [ ] 点"重算 CRC"后状态回到"CRC 通过 · N 处改动已计入校验"
- [ ] CRC 值字节(0x07E/0x07F、0x0FE/0x0FF 等)有黄框标注
- [ ] 编辑器图例色块与实际格子配色一致(红底=影响校验 / 蓝底=不影响 / 黄框=CRC 值;
      旧版曾用绿字写「蓝」, 已改成真实色块)
- [ ] 确认编辑器里的"偏移/值 原始 hex 表单"已删除(改字节直接在左侧点格子)
- [ ] 点"重算 CRC"后变更面板显示 CRC 通过
- [ ] 编辑只改内存副本: 未点"应用到设备"时总线无写事务(编辑前后各点一次"总线统计"对照;
      离线用例已证明编辑只落内存副本)
- [ ] "从设备载入"速度: 应复用最近一次读取缓存(同一地址), 不再重新读 1024B

## 4. 写入(有风险 — **必须按顺序, 严禁跳步**)

每一阶段都要求: 已保存原始 dump + 另存一份冗余备份; 目标 dump 的 CRC 校验通过。

### V0 离线(容器内, 无硬件)
- [x] `go test ./internal/...` 全绿(124 个用例, 约 2 秒)
- [x] `CGO_ENABLED=1 go test -race ./internal/...` 全绿(并发与死锁守卫)
- [x] `node --test "frontend/test/*.test.mjs"` 全绿(24 条: 页面漂移守卫 + 绑定契约)
- [x] 真实发布页面在浏览器里跑 `frontend/test/_real-page.html?v=1`, 读 `window.__SMOKE`
      (37 步全 ok、`window.__ERRORS` 为空; **必须带 query 破坏缓存**, 否则浏览器会给上一次的旧页面)
- [x] 产物自检: `go test ./internal/app/ -run TestBuiltExeEmbedsAssets`(前端与 PawnIO 模块确实打进了 exe)

### V1 只读基线
- [x] 读取 → 保存 dump → 解析 → CRC 通过(实测 1024 字节, 字读档)
- [x] "查询保护状态" 与上节一致

### V2 干跑(dry-run, 零风险)
- [ ] 勾选"干跑模式", 选一份修改过的 dump → 输入 `DRYRUN`
- [ ] 预检面板显示: 变更字节数、CRC 字节数、区域/风险表、逐字节 diff
- [ ] 执行后日志显示"将写入 N 字节; 期间总线事务: 读 X / 页选择与命令写 Y / 字节写 Z,
      其中对 SPD NVM 的数据写 0 次(零写入)" —— **把这行日志原样回帖**即 V2 的证据
- [ ] 重新读取设备内容**未变**
- [ ] (可选) 界面上干跑前后各点一次"总线统计", 确认差值与上面日志一致
- [ ] 换一根**开了 RSWP** 的条再干跑一次: 应照样给出计划(消息里带写保护提示),
      但把"干跑"取消后点写入 → 必须被拒绝并说明受保护块号
- [ ] 用一份 CRC 不通过的文件 → 干跑也必须被拒绝(数据本身不合格)

### V2.5 写入失败的回滚(离线已验, 真机可制造)

- [x] 写入中途失败(偶发 NACK)→ **自动回滚**到写入前内容并整片校验通过(离线用例
      `TestWriteAutoRollbackOnTransientFailure`)
- [x] 写"被接受但不生效"(受保护块)→ 回读校验失败 → 同样自动回滚
      (`TestWriteAutoRollbackOnVerifyFailure`)
- [x] 持续故障时回滚不可能成功 → 日志给出**备份路径**并提示手动重写(离线用例
      `TestWriteAbortKeepsCRCStale`)
- [ ] 真机: 写入失败后日志里出现"已自动回滚…", 且重新读取后内容与写入前一致

### V3 牺牲条: 单字节
- [ ] 用可牺牲的内存条; 只改序列号(或厂商)1 个字节并保持 CRC 有效
- [ ] 写入前: 自动备份路径出现在日志中
- [ ] 输入 `WRITE` 后写入 → 日志"写入并校验通过"
- [ ] 重新读取: 目标字节已变、其余字节不变、CRC 通过
- [ ] 用备份写回并确认复原

### V4 牺牲条: 扩展区(XMP/EXPO)
- [ ] 修改/新增 XMP 或 EXPO 后写入
- [ ] BIOS/系统里确认 profile 被识别
- [ ] 写入一份 CRC 故意不通过的文件 → 预检必须阻断(不写任何字节)

### V5 RSWP(可选, 不可逆性因条而异)
- [ ] RSWP 设置单块 → 状态显示该块 🔒; 重上电后仍保护
- [ ] RSWP 清除 → DDR4 走 CWP quick 命令; DDR5 写 MR12/MR13=0(可能需要离线模式/断电)

### 异常路径(任选一条牺牲条验证)
- [ ] 未装 PawnIO / 非管理员: 黄条提示, 不崩溃
- [ ] 写入过程中拔条或制造 NACK: 错误信息给出"已写 N 字节 / 未写 M 字节 + 恢复建议", 不静默半写
- [ ] 文件长度与 SPD 大小不一致: 预检直接阻断(不再有切片越界)

## 已知边界

- PSWP(永久写保护)只有 DDR2/DDR3 能用 SMBus 探测; 设置需高压(HV)编程器, 本工具不做
- DDR5 的 MR12/MR13 位在正常运行时不可清零: 解除需离线模式(MR48 bit2)或断电, 由主板/平台决定
- DDR5 时序字段在旧版未解析(本版已补), 笔记本/紧凑机型 SPD 可能挂在专用控制器上
- 部分 AM5/ITX 板 SMBus 由桥接芯片转发, PIIX4 端口可能需要两个都试
