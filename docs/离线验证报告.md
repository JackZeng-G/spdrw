# 离线验证报告(容器内, 无硬件)

> **后记(2026-09-21)**: DDR2 支持已整体移除(byte2=0x08-0x0A 现按未知类型拒绝)。下文所有 DDR2 相关条目与对应测试(TestDDR2ManufacturerBanks 等)均为当时的审计记录, 仅存档。文中各轮"顶层用例数"快照亦为当时数值(DDR2 移除后为 146)。

本报告记录**在真机之前我们到底验证了什么、结论是什么、还有什么没验**。
命令都可原样重跑; 数字来自 commit `f5d5061b`(2026-09-21)。

环境: Go 1.27.1 linux/amd64(交叉编译目标 Windows x64), 语料 67 份真实 dump。

---

## 1. 跑什么、看到什么

| 命令 | 结果 |
|---|---|
| `go test ./internal/...` | **149 个顶层用例全绿**(spd 49 / eeprom 29 / smbus 7 / app 64; 含子用例 203), 约 3 秒 |
| `CGO_ENABLED=1 go test -race ./internal/...` | 全绿(并发与死锁守卫必须过 race) |
| `node --test "frontend/test/*.test.mjs"` | **37 条全绿**(页面漂移守卫 + 绑定契约) |
| 浏览器: `frontend/test/_real-page.html?$RANDOM` | **31 步全通过, `window.__ERRORS` 为空**(必须带 query 破坏缓存, 否则读到的是上一次的页面) |
| `go test ./internal/app/ -run TestBuiltExe -v` | 通过(exe 内确实有前端页面/脚本/样式、PawnIO 模块名、PawnIOLib.dll、中文文案、`by jackzeng 2026`; 另有 `.rsrc` 节 + 版本信息 + 图标字节的独立守卫) |
| `GOOS=windows GOARCH=amd64 go build -tags desktop,production ...` | `build/SPDReaderWriter.exe`(PE32+ x86-64, 约 12MiB; 含内嵌图标与版本信息) |

### 真实语料上的关键断言(实测输出)

```
语料统计: 共 67 份(解析成功 67), CRC 通过 67 / 不通过 0, 世代分布 map[DDR3:29 DDR4:21 DDR5:17]
编辑器↔解析器一致性: 67 份真实 dump 逐字段核对完成
字段幂等检查: 通过 2737 个赋值, 0 个失败
厂商名核对: 18/18 通过
编辑→预检→干跑 全链路: 67 份真实 dump 全部通过
```

---

## 2. 每一层证明了什么

**协议层(eeprom)** —— 分页语义(EE1004 SPA / DDR5 MR11)、整片读写、写测试还原、RSWP 位图、
增量写跳过同值、**写入计划把 CRC 排在最后**、失败中止带"已写/未写"计数、干跑只落内存影子:
全部由 Fake 设备驱动, 并在 `-race` 下无数据竞争。
**三级读回退**(块读 32B → 字读 2B → 逐字节 1B)各有独立用例:只禁块读时数据仍正确且落在字读
(事务数 ≈ 字节数/2),块读字读都禁时逐字节读满整片;每级的探测都只读总线、不写。

**写入后的最终校验** —— 第三层从"只复核改动字节"升级为**整片逐字节复核**(关掉块读/字读):
读速修好后整片逐字节读只要约 1 秒(以前 32 秒), 因此付得起这个代价, 也才能发现
"计划之外的字节被改"(分页写错位、外部工具同时动总线)。`TestVerifyByteWiseCoversWholeImage`
用"只改设备不改计划"的场景证明: 只复核改动字节会漏, 整片复核能抓到。

**写入路径(唯一会变砖的操作)** —— 在全部 67 份真实 dump 上跑**真实写入**(非干跑):
备份 → 按计划逐字节写(CRC 排最后) → 逐字节回读 → 整片校验 → 设备内容与目标逐字节一致。
DDR5 的 NVM 写数**精确等于**变更字节数(保护探测只读 MR);DDR4 及更早因为预检里的写测试
会多出若干次写, 只断言"不少于变更数"。另外三条回滚用例锁住失败路径: 偶发写失败 → 自动回滚
并校验通过;写被静默忽略 → 回读校验失败 → 自动回滚;持续故障 → 回滚失败时给出备份路径。

**写入档位自适应(smbus/eeprom)** —— 与读加速对称: 逐字节写(协议 2)为先, 回读发现没写进去时
自动改试**块写(协议 5)**(模块侧 `piix4_access_block` 写路径: in[0]=长度 + 数据), 成功即切档并把
原因写进日志。`TestWriteModeFallsBackToBlockWrite` 锁住"逐字节写被忽略、块写可行"时写入仍然成功;
写入能力探测也会同时试两种档位并报告哪种生效(`TestWriteProbeDetectsBlockWriteOnly`)。
另外, 写入后的**回读固定用最原始的逐字节读**, 免得读路径的缓存/偏差把"写成功"误判成"被忽略"。

**写入能力探测(app probe)** —— 用"单字节写反值 → 回读 → 还原 → 整片复核"区分三种结局:
写入生效 / 事务被接受但内容不变(平台或 hub 拒绝) / 直接 NACK。目标字节优先选**不在 CRC
覆盖范围内**的空闲字节(断电也不会让 SPD 失效), 探测前后设备内容必须逐字节一致 ——
三条用例覆盖三种结局 + 干跑模式下拒绝探测(`TestWriteProbeVerdicts` 等)。
写入失败时错误里带**现场**(物理页/cmd + MR11/12/13/29/48/52), 并有用例锁住
(`TestWriteFailureIncludesScene`)。

**总线调优层(smbus tuning)** —— 等待模式(忙等/折中/休眠)常量与校验在可移植文件里,
Linux 下就测:`TunerOf` 必须能透过计数/记录/双层包装找到硬件会话的调优能力, 而**不能**
把一个没有该能力的 Fake 误报成"可调优"(`TestTunerOfUnwrapsWrappers`);服务层在不可调优
与未连接时都要报可读错误(`TestBusTuningUnsupportedOnFake`)。

**校验区表(CRCRanges)** —— 与校验器 CRCOK 逐字节对齐:67 份真实 dump 的 **35584 个偏移**各翻 2 个
bit,"参与校验的字节翻转后必失败、不在范围内的字节翻转后必须仍通过"全部成立(仅跳过决定世代的字节)。
另按 JEDEC 布局断言:DDR4/DDR5 的身份区(序列号/生产日期/部件号)不在校验范围内,DDR3 的序列号在、
部件号不在 —— 这正是"改序列号却提示重算 CRC"那个问题的判定依据。

**解析层(spd)** —— 67 份真实 dump 全部识别正确、CRC 全部通过; DDR4 JEDEC/ XMP 2.0、
DDR5 JEDEC/XMP 3.0/EXPO 的字段用**厂商型号里公开的规格**做外部参照核对
(Corsair 5600 CL40 1.25V、GeIL 8000 CL38 1.45V、G.Skill EXPO 6000 1.35V、
Viper4 3200 的 tRAS/tRC 36/64 周期…)。

**编辑层(editor)** —— 与解析层是两套独立实现, 交叉校验要求两边对同一根条给出同一个值;
每个字段"改一次再改回"必须逐字节复原(唯一例外是时序的等价编码, 按时间等价判定);
字段写入不得越界到 CRC 字节或相邻字段。

**服务层与界面(app/frontend)** —— 绑定契约(≤2 返回值、参数形态)、写入预检与确认串、
备份、干跑、总线事务计数、编辑器标签页、写保护面板: Node 契约测试 + **真实浏览器**跑真实
发布页面(`make-real-page.mjs` 从 `dist/index.html` 生成, 不是骨架副本)。

**产物** —— exe 内嵌前端与 PawnIO 模块的静态自检通过。

---

## 3. 审查发现的问题(已全部修复)

先后请两个"新鲜上下文"的子代理做独立正确性审查。第一轮找出 10 个问题, 其中两条高严重度:

1. **XMP 2.0 的 tRAS/tRC 高位 nibble 位序错了**(沿用了上游实现 C# 的错误, 且 tRC 丢了高位)——
   改一次 tRAS 会把真实 tRC 从 40ns 清成 8ns, 而 XMP 区不在 CRC 覆盖内, 校验照样通过。
   我用自己的独立计算 + 厂商规格确认了审查是对的(按旧位序是 87/103 周期, 按 JEDEC 是 36/64、39/64)。
2. **干跑/预检的写保护探测真的写总线**(DDR4/更早靠"取反写一字节再还原"), 而"零 NVM 写"
   统计窗口却从预检之后才开始 —— 既真写了又报告"零写入"。

其余: 并发(共享 Editor map 与 Device 分页状态无保护)、XMP2 CL 越界写入、XMP2 tRFC 单位错 8 倍、
空 XMP3 槽不补 CRC、`EditVerifyFile` 越界 panic、"幽灵变更"、DDR3 FTB 整数除法、两处
`CRCOffsets` 判定漂移。修复过程中我自己还犯过一次锁嵌套(自锁死), 靠 `-race` 跑到超时才发现,
现已加机械守卫。详见 `docs/实现文档.md` 的"独立审查发现并修掉的问题"一节。

### 第三轮: 写入路径对抗性审计(2026-09-21, 真机写入前)

请第三个子代理**只针对"会不会把条写坏"**做对抗性审查(读代码 + 用 Go overlay 注入故障复现)。
它找出 **1 个阻断项 + 2 个高危 + 5 个中危 + 6 个低危**, 全部已修并有用例钉住:

| 级别 | 问题 | 修法 |
|---|---|---|
| **阻断** | **备份晚于写保护探测**: DDR4/更早的探测本身是真实 NVM 写(块首取反写→还原)。还原写一失败, byte0 被永久改坏, 而备份里存的也是坏值, 回滚拿坏值比对自己还报"无需回滚" | 备份提到**任何探测之前**;探测后整片复核, 不一致立即回滚并**中止**写入(`TestBackupHappensBeforeProbeWrite`) |
| 高 | **世代不对照**: 只比长度与 dump 自身 CRC。256B 的 DDR3 镜像能写进 DDR2 条(反之亦然) | 预检与写入两道门都比对 `spd.Identify(dump)` 与设备类型, 不一致/类型未知一律阻断(`TestGateDumpRejectsCrossGeneration`) |
| 高 | **"查询保护状态"是无备份无确认串的真实写**; RSWP 加/清除只看 quick 是否 ACK | 查询前先备份 + 探测后复核; WPSet/WPClear 要确认串(`RSWP`/`CLEAR`)并**回读复核**是否真的生效(`TestWPSetClear`) |
| 中 | `writeWithPreflight` 只信调用方传来的预检; `WriteConfirmed` 读两次文件(TOCTOU) | 只读一次并一路复用; 内部独立复检长度/世代/CRC(`TestWriteWithPreflightGatesDataItself`) |
| 中 | **预览会写设备**: 干跑影子建立失败时错误被吞, 照跑 DDR4 写测试(实测 8 次数据写) | 建影子失败即中止预览, 不探测(`TestPreviewNeverWritesEvenWhenShadowFails`) |
| 中 | "NVM 写"从有上限(512)的写日志里数, force 写 1024 字节会少报一半 | 传输层独立累计(`TestNVMWriteCountNotTruncated`) |
| 中 | BIOS "SPD Write Disable" 读了但从不参与门禁 | 预检直接按 `bios` 阻断, 干跑放行但带警示(`TestBIOSWriteDisableBlocksRealWrite`) |
| 低 | MR11 整字节写会清 bit3(寻址模式位); 备份名秒级精度同秒互相覆盖; 逐字节回读与整片校验共用同一条读路径; 回读阶段"已写"少算 1; 空计划也写备份 | 全部修正(读-改-写 MR11 并保留高位; 备份名加纳秒; 改动字节再用**逐字节**读法复核; 计数修正; 空计划直接返回) |

审计同时判定 5 个类别**干净**并给了证据: CRC 最后写、写后校验、分页语义、
干跑(除上述预览缺陷)、并发(opMu 覆盖), 以及 force/增量模式与确认串。

### 第四轮: 全后端审计(2026-09-21, 真机 V4 通过之后)

真机写入链路跑通后, 请**三个新鲜上下文的子代理并行**只读审计后端, 按层分工:
`internal/eeprom` + `internal/smbus`(总线/器件)、`internal/spd`(解析/编辑器)、
`internal/app`(编排/并发/门禁)。它们互不通气, 结论交叉对照; 我逐条复核后修掉能修的,
剩下的列为"已知未修"并在本节末尾明写。

**修掉并有用例钉住的**

| 层 | 级别 | 问题 | 修法 |
|---|---|---|---|
| app | 高 | **`AutoConnectAll` 用过滤后列表的下标当控制器 id** —— 用户机器上有 5 个控制器、其中 4 个 id 重复, 过滤掉无 SPD 的之后下标与 `ControllerInfo.Index` 不再相等, 会连到另一个控制器(或连不上) | 全程用 `ctls[i].Index`; 前端 `option.value` 同步用 `c.index`(`TestAutoConnectUsesControllerIndex`) |
| app | 高 | **`Scan()` 空指针 panic**: 设备无响应时 `lastErr[0x50]` 不存在就解引用 | 取第一个非 nil 错误(`TestScanNoDeviceNoPanic`) |
| app | 高 | **预览被未知块门禁挡死**: 未知块检查设在所有路径上, 正常预览直接失败 | 门禁只在 `probeProtection` 为真时启用(`TestPreviewNotBlockedByUnknownBlock`) |
| app | 高 | **`SaveDump` 持 `opMu` 又调内部函数二次加锁 → 自死锁**(`deadlock_test.go` 的机械守卫当场抓到) | 抽出自持锁的 `dumpLocked`, 公开方法只加一次锁 |
| app | 中 | `EditFixCRC` 对**CRC 本来就通过**的 dump 也重算并标"已改动", 67 份真实 dump 里有 6 份被无谓修改 | 已通过即 no-op(`TestEditFixCRCRefusesHealthyDump`) |
| app | 中 | `EditLoadFromDevice` 命中缓存就不再重读; 文件来源的编辑器内容无法写回 | 永远重读; 允许文件来源内容写入(`TestEditLoadFromDeviceAlwaysRereads`) |
| app | 中 | 读取失败仍把编辑器/缓存留在"上一次成功"的状态 | `Dump` 失败清缓存; 编辑器未脏时自动接管(`TestDumpKeepsDirtyEditor`) |
| eeprom | **阻断** | **256B 单页器件(DDR2/DDR3)也会走 SPA 写 0x36 选页** —— 0x36 在单页器件上是 SWP/PSWP 地址空间, 既让 DDR2/DDR3 直接 NACK 读不出来, 又有误改保护位的风险 | 页数 ≤1 时 `pageKnown=true`, `setPage` 直接返回, `physOffset` 不再切换; 非 DDR5 且尺寸 >512 → `typeKnown=false` 且尺寸归一为 512(`TestSinglePageNeverWritesSPA`) |
| eeprom | 高 | 恢复/复核路径用"可能被加速过的"块读/字读比对, 假一致会放过坏写 | `afterProbeCheck`/`restoreImage`/`WriteProbe` 全部改用逐字节读法复核 |
| eeprom | 高 | **干跑没拦 RSWP 加/清除** —— DDR4 上 RSWP 是真实命令 | 器件层与 app 层双重拒绝(`TestDryRunBlocksRSWP`) |
| eeprom | 高 | MR11 读失败时仍然盲写整字节(会清掉 bit3 寻址模式位) | 读失败即放弃该次页选, 不再盲写 |
| eeprom | 中 | `VerifyChangedByteWise` 越界 panic; `ApplyWrite` 偏移被截成 uint16 后**静默写到错误地址** | 全部加边界校验(`TestApplyWriteRejectsOutOfRange`) |
| smbus | 中 | `n<0` 的返回长度未钳位; 25ms 写周期延时**算在 DDR5 寄存器写**上(DDR5 寄存器写不需要) | 钳位; 按世代判断是否真需要写周期(`needsWriteCycle`) |
| smbus | 中 | `NVMWrites` 在 DDR4 少数写入(`cmd==0 && val==0` 被当寄存器写漏掉) | 计数与记录两层都按世代区分 |
| smbus | 中 | Fake 对 0x30–0x3F 恒 ACK、MR11 写不可观测 → 上述两个 bug 在离线测试里**测不出来** | Fake 补齐: MR11 写会真的改页状态, 0x30–0x3F 不再恒 ACK |
| smbus | 低 | `pawnio_dll` 的 `close` 重复调用不幂等 | 幂等化 + `lockSMBus` |
| spd | 高 | **短/截断 dump 解析 panic**(基本区长度、DDR4 时间基、容量除零、密度表越界) | 全部设长度门与除零守卫(`TestParsersNeverPanic`) |
| spd | 高 | **`SetField` 接受别世代的字段键** —— 能 panic, 也能"合法地"把 DDR5 值写进 DDR4 结构 | `setProfileField` 加世代门(`isDDR5Family`/`isDDR4Family`)+ 取值域检查(`TestSetFieldGenerationGate`) |
| spd | 中 | EXPO profile 的使能位取错(P2 是 bit4, 之前按 bit1 走) | 修正位序 |
| spd | 中 | "存在"开关是破坏性的(关掉会清字段), 且会产生幽灵变更 | 改为非破坏性, 在真实 dump 上验过(`TestPresentTogglesNonDestructive`) |
| spd | 中 | DDR2 厂商码/版本号顺序/tCK 分数码(8b/12b)错误; 银行号 >7 时越界写 | 按 JEDEC 修正, 越界拒绝(`TestDDR2ManufacturerBanks`) |
| spd | 低 | JEP106 表每次手写重建(且重名查找不可靠) | `sync.OnceValue` 惰性构建 |

**修不掉的、明写在案的(不隐藏)**

| 层 | 问题 | 为什么先不修 |
|---|---|---|
| eeprom | 读回镜像缺**独立**交叉校验(逐页读比对/自算 CRC) | 现有三层校验已覆盖"写进去的是不是对的"; 独立读数交叉校验要额外一倍事务量, 真机上 1024B 读已 ~320ms, 待有实际误读证据再加 |
| eeprom | DDR4 的 SPA 选页不做 NACK 容忍, 也不用 0x36 回读验证 | Linux `ee1004.c` 记录了 ACK/NACK 探测法; 需要真实 DDR4 条才能定策略, 现无硬件 |
| smbus | `lockSMBus` 拿不到锁时**静默继续**(无错误传播) | 需要改接口签名(返回 error)并逐层处理; 当前实现下锁只在多实例场景失效, 单实例正常 |
| app | 备份文件的**原子性/保留策略**(写入的中途崩溃可能留半份备份, 且不清理旧备份) | 影响的是"坏了之后能不能回滚", 不影响写前正确性; 列为下一轮 |
| smbus | `MarshalXfer` 对 >33B 的块写**静默截断** | 当前所有调用点都已按 `BlockMaxPayload=32` 切分; 停在"约定"而非"强制", 应改成返回错误 |
| eeprom | `Device` 自身不加锁(依赖 app 层 `opMu`) | 单入口调用下安全; 作为库单独使用时不安全, 已在实现文档注明 |
| 其他 | MR48 bit2/bit3 文档表述不一致; BCD 非规范 nibble 往返; 无操作写入返回 `res.Verified=false`; `writeWithPreflight(img=nil)` 的潜在顺序问题 | 低危/纯文档, 已记录 |

> 本轮改动规模: 34 个文件、+1945 / −473 行(含 3 个新增测试文件)。
> 验证(当轮快照): `go vet ./internal/...` 干净; `go test ./internal/... -count=1` **144 个顶层用例(209 含子用例)全过**;
> `CGO_ENABLED=1 go test -race ./internal/... -count=1` 全过; `node --test "frontend/test/*.test.mjs"` **35 个全过**;
> 真实浏览器冒烟(从发布版 `dist/index.html` 生成的 `_real-page.html`)**31 步全 ok、`window.__ERRORS` 为空**;
> Windows exe 重新交叉编译, `TestBuiltExeEmbedsAssets` 通过。

### 第五轮: 绑定面收敛与静默失败清理(2026-09-21)

真机验证与审计都过了之后做的"减面"清理。目标不是加功能, 而是**让暴露出去的东西都必要、
让失败都出声**。

**① 导出绑定 50 → 32**(前端只用 29 个, 另 3 个是 main.go 的 Wails 生命周期)

| 处理 | 方法 | 理由 |
|---|---|---|
| 删除 | `SaveDump` / `SaveDumpDialog` / `SaveDumpData` / `DecodeFile` / `DecodeFileDialog` / `ReadFileBytes` / `PickWriteFile` / `ResetBusStats` / `SetFastRead` / `SetSleepMode` / `SetDryRun` | 全是历次 UI 精简后没人调的旧入口; 设备级的同名能力(分页/等待模式/干跑)仍在内部被写路径使用 |
| 包内化 | `writeConfirmed` / `preflightWrite` | **关键**: 它们是"按文件路径写设备", 留着等于前端还有第二条绕过编辑器的写入路径 |
| 包内化 | `busStats` / `logEntries` / `closeDevice` / `saveDump` / `decodeFile` / `verifyFile` / `editLoadPath` | 由更外层绑定调用, 或只给测试用 |
| 复活 | `Close` | 原本无人调用(死绑定), 改挂 Wails `OnShutdown`: 关窗即释放设备会话与全局 SMBus 互斥量 |

**② 三处静默失败改成出声**

- `MarshalXfer`: 块写入参 >33 字节(1 长度字节 + 32 数据)或长度字节与实际数据不符 → **返回错误**。
  以前模块只认前 33 字节, 多出来的被悄悄丢掉, 调用方以为全写进去了。
- `lockSMBus`: 创建全局互斥体失败时以前返回空 unlock(等于"不做仲裁却假装有")→ 现在
  `(unlock, error)` 且 **fail-closed**: 拿不到仲裁就拒绝本次事务, 并报出原因(创建失败/等待失败/超时)。
  写 SPD 时"两个工具同时驱动一条总线"是最不该出现的场景。
- 备份落盘: 临时文件 + fsync + rename(中断只留 `*.tmp`, 不会留半份"看起来像备份"的文件)、
  rename 后**回读逐字节校验**、内容为空直接拒绝、只保留最近 20 份并清理残留 `*.tmp`。

**③ 静态检查进流程**: `deadcode -test` 与 `staticcheck -checks=all` 都归零(跑法见 `实现文档.md` §7);
前端没有 lint, 由 `page.test.mjs` 承担同类守卫(id 漂移、绑定桩缺失、驱动元素缺失、色值重复、sticky 失效)。

> 验证(本轮): `go vet` / staticcheck / deadcode 全 0; `GOOS=windows` 构建与 vet 通过;
> `go test ./internal/...` **148 个顶层用例全绿**; `-race` 全绿; 前端 **37 条**全绿;
> 浏览器冒烟 31 步全 ok 且无控制台错误; exe 重建并通过内嵌自检。
> 新增用例: 原子写不留残、备份保留最新且不动非本工具命名的文件、真实备份路径回读一致且两次不覆盖、
> 块写超长/长度字节不符/未知协议必须报错。

---

### 第六轮: 构建目录/图标/署名收尾(2026-09-21)

- **产物目录 `bin/` → `build/`**(用户要求): `.gitignore` 只忽略 `build/*.exe` 等产物,
  `build/icon/`(图标源)与 `build/winres/`(Windows 资源配置)是**源文件**, 要入库;
  同时新增 `.gitattributes` 固定行尾(文本 LF、`.ps1` CRLF)并把 exe/dll/syso/ico/png 标记为二进制。
  构建入口统一成 `./build.sh`(Linux/macOS 交叉编译)与 `.\build.ps1`(Windows), 两者等价。
- **图标**: `tools/makeicon`(纯标准库, 不依赖 ImageMagick/PIL)画"深蓝圆角方块 + 绿色内存条 +
  斜放铅笔"; 4 倍超采样抗锯齿, 16/24 像素用简化版(去铅笔、加粗金手指)。产物 `build/icon/`
  下是 512 PNG + 多尺寸 ICO + 各尺寸 PNG(go-winres 只吃 PNG)。
- **Windows 资源**: `build/winres/winres.json` 经 go-winres 生成仓库根目录的
  `rsrc_windows_amd64.syso`, 于是 exe 有图标、属性里有产品名/版本/版权 `by jackzeng 2026`。
  随后补上**清单**: `requireAdministrator`(双击自动弹 UAC, 免得手点"以管理员身份运行")+
  与 `wails build` 同款的 DPI 感知(`permonitorv2`)与 Common-Controls v6。
- **界面**: 写入区标题 `变更与写入` → **`写入SPD芯片`(红色)**; 顶栏品牌右侧加 `by jackzeng 2026`
  (窄窗自动隐藏); 启动日志首行也带署名。版权页同步到 `LICENSE` 与 `wails.json`。
- **机械守卫**: `TestBuiltExeHasWindowsResources`(exe 里必须有 `.rsrc` 节、UTF-16 版权串、
  256px 图标字节)与 `page.test.mjs` 的两条新断言(署名存在、写入标题是 `--danger` 红)。

> 验证(本轮): 产物自检两条全过; `go test ./internal/...` **149 个顶层用例全绿**;
> `-race` 全绿; 前端 **38 条**全绿; 浏览器冒烟 31 步全 ok。

---

## 4. 这套离线验证**没有**覆盖什么(必须真机确认)

| 未覆盖 | 为什么只能真机 | 对应验证步骤 |
|---|---|---|
| 真实 SMBus 时序/仲裁 | Fake 不模拟总线电气与控制器重试; Thaiphoon/OpenRGB 同时占用时的表现 | V1 |
| 真实颗粒的写周期与保护行为 | `eepromWriteDelay(25ms)`、NACK/超时处理、RSWP 探测在实物上的表现 | V3 ✓ / V5 |
| BIOS 是否接受修改后的 SPD | 容量/时序/扩展区的解释权在固件 | V4(SPD 侧 ✓, 固件侧待确认) |
| 干跑在真机上确实零写入 | 离线只能证明代码路径; 真机用**总线计数**读出来才算证据 | **V2 ✓** |
| Windows/WebView2 运行时 | 容器内只有 Chromium + DOM 桩, 不是 Wails 的真实运行时 | V1 起 |
| DDR2 | 公开渠道**没有任何**真实 dump(已穷举 coreboot 全量 `.spd.hex` + 多轮仓库搜索), 只有合成样本覆盖 | 遇到 DDR2 先只读 |
| 字读(2B/事务)在本机 HUB 上的表现 | 探测靠"字读结果 == 两次字节读结果"比对, 只读且失败即降级, 但**是否被接受只有真机能答** | V1 看日志档位 |

---

## 5. 下一步(需要你)

1. ~~**V1 只读**~~ **已通过**: 枚举控制器 → 扫描 → 选条 → 读 dump, 类型/容量/厂商/部件号与 CPU-Z 一致,
   CRC 通过, 保护状态正常; 读速已从 16.5s 修到 ~320ms(见 `docs/真机验证.md` 第四轮)。
2. ~~**V2 干跑**~~ **已通过**: 真机 `… 其中对 SPD NVM 的数据写 0 次(零写入)`。
3. ~~**V3 单字节真写**~~、~~**V3.5 校验区内改动 + 重算 CRC(双向)**~~、~~**V4 CRC 最后写**~~ **均已通过**,
   证据见 `docs/真机验证.md` 第七、八轮与 `docs/真机验证.md`。
4. **待做(可选)**: **V5 RSWP**(加/清除; 部分颗粒不可逆)、扩展区 XMP/EXPO 生效性、异常路径
   (未装 PawnIO / 写入中途 NACK / 文件长度不符)。逐步步骤见 `docs/真机验证.md` 第 4 节。

> 状态(2026-09-21 更新): 全部实现已合并到 `main`, 当前分支含第四轮全后端审计的修复(34 文件, +1945/−473)。
> 真机已确认: V1 读取 + 写保护状态; 编辑器改字段生效; 读速修复(1024B 单次 ~320ms, 392.9kHz 忙等);
> **V2 干跑零写入、V3 单字节真写、V3.5/V4 校验区内改动 + CRC 重写(双向)** 全部通过。
> 仍待真机确认: V5(RSWP)与扩展区 profile 的固件侧生效性。
